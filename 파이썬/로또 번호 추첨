import random as r
for j in range(10):
    result = []
    for i in range(7):
        numbers = r.randint(1,50)
        if numbers in result:
            numbers = r.randint(1,50)
            result.append(numbers)
        else:
            result.append(numbers)
    result.sort()
    print(result)
