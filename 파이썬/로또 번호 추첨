import random as r

for j in range(10):
    result = []
    while len(result) < 7:
        numbers = r.randint(1,50)
        if numbers not in result:
            result.append(numbers)
            
    result.sort()
    print(result)
